name: Build and Sign IPA

on:
  push:
    branches: [ "main" ] # Cambia a "master" si ese es el nombre de tu rama principal

jobs:
  build-ios:
    runs-on: macos-14 # Esto garantiza Xcode 15/16 (Requerido por Apple en 2024/2025)
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          # Puedes especificar tu versión de flutter aquí si es necesario

      # 1. INSTALAR CERTIFICADOS (Usando tus Secrets)
      - name: Install Apple Certificate and Provisioning Profile
        env:
          P12_CERT: ${{ secrets.P12_DISTRIBUTION_CERTIFICATE }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          PROVISIONING_PROFILE: ${{ secrets.PROVISIONING_PROFILE }}
        run: |
          # Crear variables de ruta
          CERT_PATH=$RUNNER_TEMP/build_cert.p12
          PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          # Decodificar de Base64 a archivos reales
          echo -n "$P12_CERT" | base64 --decode > $CERT_PATH
          echo -n "$PROVISIONING_PROFILE" | base64 --decode > $PP_PATH

          # Crear e importar al keychain temporal del servidor
          security create-keychain -p "" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "" $KEYCHAIN_PATH
          security import $CERT_PATH -k $KEYCHAIN_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -Z
          security list-keychain -d user -s $KEYCHAIN_PATH

          # Instalar el Perfil de Provisión
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/

      # 2. SOLUCIONAR ERROR DE PODS (Usando el CDN)
      - name: Install Dependencies and Pods
        run: |
          flutter pub get
          cd ios
          # Forzamos el uso del CDN para evitar el error que tenías
          rm -rf Pods Podfile.lock
          pod repo add-cdn master https://cdn.cocoapods.org/
          pod install --repo-update
          cd ..

      # 3. COMPILAR EL IPA
      - name: Build IPA
        run: |
          flutter build ipa --release \
            --export-method app-store \
            --build-number ${GITHUB_RUN_NUMBER}

      # 4. SUBIR EL ARCHIVO FINAL (Para que lo descargues)
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-ventas-release
          path: build/ios/ipa/*.ipa